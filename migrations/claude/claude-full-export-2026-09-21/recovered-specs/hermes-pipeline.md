# Hermes pipeline — recovered specification

Evidence level: persistent-memory summary; original v0.1 plugin bundle absent.

Recovered historical flow: context feed -> ideate -> approval -> sandboxed build/run -> report -> pick winners -> handoff. Permanent ideas are retained in a ledger; deduplication prevents repeated ideas; builds run in a sandboxed worker environment.

Later direction: the real Hermes desktop runtime becomes the production control plane, with model/API adapters inside Hermes and external AI assistants used primarily to author/improve agents.

Migration treatment: architecture recovered into `hermes-agent-creator/references/recovered-hermes-runtime.md`; do not claim the old plugin's executable files are present.
