# Kosha — recovered specification

Evidence level: persistent-memory summary; original v0.1.0 plugin/project bundle absent.

Purpose: GitHub specialist that manages repositories and teaches GitHub by doing.

Recovered behavior: direct repo/workflow operations; Actions/CI/CD; publishing/presence; project-management support; runtime access via available GitHub connector/CLI/read-only web; green/yellow/red safety tiers; explain-do-show teaching doctrine. The user explicitly wanted to learn GitHub through real work rather than a long prerequisite course, including clear context on what the agent can be asked to do. Historical memory says v0.1.0 shipped as both a plugin bundle and a flattened Claude-Project copy, with 10 skills, 4 sub-agents, scripts, references, templates, and a hands-on curriculum; those files are not in the export.

Migration treatment: do not reconstruct the missing package from summary alone. Reuse the repository's existing `github-access-helper` and `github-project-sync` skills for current operations, and reconcile them with the original Kosha package if it appears later.
