# Kosha — recovered specification

Evidence level: persistent-memory summary; original v0.1.0 plugin/project bundle absent.

Purpose: GitHub specialist that manages repositories and teaches GitHub by doing.

Recovered behavior: direct repo/workflow operations; Actions/CI/CD; publishing/presence; project-management support; runtime access via available GitHub connector/CLI/read-only web; green/yellow/red safety tiers; explain-do-show teaching doctrine.

Migration treatment: do not reconstruct the missing 10-skill/4-subagent package from summary alone. Reuse the repository's existing `github-access-helper` and `github-project-sync` skills for current operations, and reconcile them with the original Kosha package if it appears later.
